// Find the sum of the array.
// Note: Don’t use any loop
// const numbers = [1, 2, 3, 4];

const numbers = [1, 2, 3, 4];



    total   = numbers.reduce((curr, prev) => 
    { return curr + prev;
     });

console.log(total);
